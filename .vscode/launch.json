{
  "configurations": [
    {
      "type": "cppdbg",
      "request": "launch",
      "name": "Debug testSuite",
      "program": "${workspaceFolder}/tests/testSuite",
      "cwd": "${workspaceFolder}",
      "args": [],
      "environment": [
        { "name": "LD_PRELOAD", "value": "${workspaceFolder}/build/libGL.so" }
      ],
      "MIMode": "gdb"
    }
  ]
}
